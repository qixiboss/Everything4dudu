{
  "name": "everything4dudu",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js tests/**/*.test.js",
    "check": "node scripts/check-integrity.js",
    "build": "node scripts/build-site.js",
    "verify": "npm run build && npm run test && npm run check"
  },
  "engines": { "node": ">=22" }
}
