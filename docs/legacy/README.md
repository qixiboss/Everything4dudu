# 历史应用资料

此目录保留单一门户架构迁移前的应用说明、维护笔记、许可证和音频对齐资料，供查阅历史设计时使用。

这些文件不参与 GitHub Pages 发布、`npm run verify` 或当前应用行为定义。当前唯一运行源码是仓库根目录的 `site/`；当前开发约定见根目录 `README.md` 与 `CLAUDE.md`。

旧 WordTales 的整档 `learning_profiles` 云同步测试、原始完整性脚本和原始迁移未归档，因为当前门户已经改用按条目同步，且根目录测试与完整性检查覆盖现行行为。
