# 2026-08-18

- 从 `site/words/js/data.js` 提取全部单词，生成 3 列 PDF 背诵表《单词背诵表.pdf》：单词 / 音标 / 中文意思，共 897 词、53 页，按词集与列分组。使用 Arial Unicode 字体嵌入，确保 IPA 音标与中文在 iPad 上都能正常显示。
- 调整版：改为「每列（一个数据分组）占满一页」的布局，共 28 页（每列对应一整页），无跨列翻页；每页字号按该列词数自动缩放以填满整页，密集列（37 词）字号约 11pt，稀疏列（18 词）字号约 14pt。
