# 2026-08-19

- WordTales 新增「星标单词」功能：记录表旁加入口按钮，overlay 面板按正文顺序展示全部星标词条（单词/音标/词性/释义/词集·栏目/标记时间），右上角导出按钮可勾选列导出带 BOM 的 CSV。
- 新模块 `site/words/js/starred-words.js`（WordTales.StarredWords，参照 StudyRecord overlay 模式，只读 LearningProgress）；同步改动：index.html（按钮、script 标签、changelog v3.8、CSS v3.0.8）、styles.css（starred-* 样式）、app.js（init）、check-integrity.js（expectedScripts）、新增 tests/wordtales/starred-words.test.js。npm run verify 全绿（83 tests）。
- 测试坑：vm 上下文返回的数组跨 realm，node:test 的 deepStrictEqual 会判不相等，需用 Array.from 转回测试侧再比较。
- 导出菜单新增「筛选词集」：第一份~第七份 7 个复选框（默认全选），导出时按词条在所有专栏的出现范围（row.setIds）过滤，跨词集同词条（如 radiate 在 set1+set6）仍可命中。菜单分两段（词集/导出列），各自独立全选/清空。starred-words.js → v1.1.0、CSS → v3.0.9，changelog v3.8 描述同步更新。新增 filterRowsBySets/getSetFilters 测试，npm run verify 84 tests 全绿。
